<h1>Edit Post</h1>
<div id="page">
	<%= form_for @post do |p| %>
		<%= p.label :title %>
		<%= p.text_field :title %>
		<%= p.label :content %>
		<%= p.text_area :content %>
		<%= p.submit %>
	<% end %>
</div>